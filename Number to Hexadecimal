class Solution:
    def toHex(self, num: int) -> str:
        if num == 0:
            return "0"

        num &= 0xffffffff

        digits = "0123456789abcdef"
        ans = ""

        while num:
            ans = digits[num & 15] + ans
            num >>= 4

        return ans
